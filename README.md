# Maze-Ai
**Maze AI Solver** is a simple web app that demonstrates how AI finds a path in a maze. The maze is displayed as a grid in the browser. When the user clicks **Solve Maze**, the Python (Flask) backend calculates the path and sends it to the frontend. The solution is then animated on the maze using HTML, CSS, and JavaScript.
